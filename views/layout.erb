<%# 
Cistergraphia Layout Template
============================

Purpose:
- Provides consistent styling and structure
- Implements responsive design
- Includes common UI elements

Design Decisions:
- Comic Sans for playful aesthetic
- Gradient background for visual interest
- Card-based container layout
- Animated logo for engagement
- Mobile-friendly design

Constraints:
- Must work across modern browsers
- Responsive down to 320px width
- Maintains consistent spacing
- Handles dynamic content
%>
<!DOCTYPE html>
<html>
<head>
  <title>Cistergraphia</title>
  <style>
    .logo {
      display: block;
      margin: 0 auto;
      width: 120px;
      height: 120px;
      animation: spin 4s linear infinite;
    }
    @keyframes spin {
      from { transform: rotate(0deg); }
      to { transform: rotate(360deg); }
    }
  </style>
  <style>
    body {
      font-family: 'Comic Sans MS', cursive, sans-serif;
      max-width: 800px;
      margin: 0 auto;
      padding: 20px;
      background: linear-gradient(135deg, #f5f5f5, #e0e0e0);
    }
    .container {
      background: #fff8e1;
      padding: 20px;
      border-radius: 12px;
      box-shadow: 0 4px 8px rgba(0,0,0,0.2);
    }
    form {
      margin: 20px 0;
    }
    input[type="text"] {
      width: 100%;
      padding: 10px;
      margin: 10px 0;
      border: 2px solid #ff9800;
      border-radius: 8px;
      background: #fff3e0;
    }
    input[type="submit"] {
      background: #ff5722;
      color: white;
      padding: 12px 24px;
      border: none;
      border-radius: 8px;
      cursor: pointer;
      transition: background 0.3s;
    }
    input[type="submit"]:hover {
      background: #e64a19;
    }
    .svg-container {
      overflow-x: auto;
      margin: 20px 0;
      padding: 10px;
      background: #fff3e0;
      border: 2px solid #ff9800;
      border-radius: 8px;
    }
    .error {
      color: #d32f2f;
      background-color: #ffcdd2;
      border: 2px solid #e57373;
      padding: 15px;
      border-radius: 8px;
      margin: 20px 0;
    }
  </style>
</head>
<body>
  <div class="container">
  <svg class="logo" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100">
    <circle cx="50" cy="50" r="48" stroke="black" stroke-width="2" fill="white"/><!-- Face -->
    <circle cx="35" cy="40" r="5" fill="black"/><!-- Eyes -->
    <circle cx="65" cy="40" r="5" fill="black"/><!-- Eyes -->
    <path d="M 35 65 Q 50 80, 65 65" stroke="black" stroke-width="2" fill="transparent"/><!-- Mouth -->
  </svg>
  <h1 style="text-align: center; color: #ff5722;">Cistergraphia</h1>
    <%= yield %>
  </div>
</body>
</html> 